# 🔑 AWS Credentials for AirSign
# Copy this file to .env and add your actual AWS credentials

# AWS Configuration
VITE_AWS_ACCESS_KEY_ID=your_access_key_here
VITE_AWS_SECRET_ACCESS_KEY=your_secret_key_here
VITE_AWS_REGION=us-east-1

# Instructions:
# 1. Create AWS account at https://aws.amazon.com/
# 2. Create IAM user with Bedrock, IoT, SageMaker, Lambda, SNS permissions
# 3. Generate access keys
# 4. Replace the values above with your actual keys
# 5. Rename this file to .env

# Security Note:
# Never commit .env files to version control
# These credentials are for demo purposes only